import styled from "styled-components";

const ImageAvatar = styled.img`
    width: 100px;
    height: 100px;
    border-radius: 50%;
`

const MySpan = styled.span`
  cursor: pointer;
  font-size: 20px;
`

export const S = {
    ImageAvatar,
    MySpan
}